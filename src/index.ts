// TODO(M1): replace with the real bootstrap (Express app, Mongo/Redis connect, jobs, pino).
// Placeholder so `tsc`/`typecheck` have an input before the first module.

export const serviceName = 'ai-service';

process.stdout.write(`${serviceName}: scaffold ready\n`);
